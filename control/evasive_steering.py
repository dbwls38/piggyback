class EvasiveSteering:

    def avoid(
        self,
        direction="left"
    ):

        print()

        print(
            f"[EVASIVE STEERING -> "
            f"{direction.upper()}]"
        )